import { Building2, Clock3, Phone, UserRound } from 'lucide-react'
import type { Department } from '../types'

export function DepartmentCard({ department }: { department: Department }) {
  return <section aria-label="담당 부서 정보" className="mt-4 rounded-xl border border-blue-100 bg-blue-50/70 p-4"><div className="mb-3 flex items-center gap-2 text-sm font-bold text-slate-800"><Building2 size={17} className="text-brand-600"/>담당 부서 안내</div><dl className="grid gap-2 text-sm"><div className="flex gap-2"><dt className="w-20 shrink-0 text-slate-500">담당 부서</dt><dd className="font-semibold text-slate-800">{department.name || '확인 필요'}</dd></div>{department.contactPerson&&<div className="flex gap-2"><dt className="flex w-20 shrink-0 items-center gap-1 text-slate-500"><UserRound size={13}/>담당자</dt><dd>{department.contactPerson}{department.contactRole ? ` (${department.contactRole})` : ''}</dd></div>}{department.contactDuty&&<div className="flex gap-2"><dt className="w-20 shrink-0 text-slate-500">담당 업무</dt><dd>{department.contactDuty}</dd></div>}<div className="flex gap-2"><dt className="flex w-20 shrink-0 items-center gap-1 text-slate-500"><Phone size={13}/>전화번호</dt><dd className="font-semibold text-slate-900">{department.phone || '원문에서 확인되지 않음'}</dd></div>{department.email&&<div className="flex gap-2"><dt className="w-20 shrink-0 text-slate-500">이메일</dt><dd>{department.email}</dd></div>}{department.officeLocation&&<div className="flex gap-2"><dt className="w-20 shrink-0 text-slate-500">사무실</dt><dd>{department.officeLocation}</dd></div>}{department.officeHours&&<div className="flex gap-2"><dt className="flex w-20 shrink-0 items-center gap-1 text-slate-500"><Clock3 size={13}/>운영시간</dt><dd>{department.officeHours}</dd></div>}</dl>{department.contactSource&&<p className="mt-3 border-t border-blue-100 pt-3 text-xs text-slate-500">{department.sourceUrl?<a className="font-semibold text-brand-700 underline underline-offset-2" href={department.sourceUrl} target="_blank" rel="noreferrer">{department.contactSource}</a>:department.contactSource}</p>}</section>
}
