import { ExternalLink, FileText } from 'lucide-react'
import type { Notice } from '../types'
import { noticeStatusPresentation } from '../utils/noticeStatus'

export function NoticeCard({ notice }: { notice: Notice }) {
  const status = noticeStatusPresentation(notice)
  return <article className="mt-2 rounded-xl border border-slate-200 bg-white p-3 text-left transition hover:border-brand-300 hover:shadow-sm"><div className="flex items-start gap-3"><span className="grid h-9 w-9 shrink-0 place-items-center rounded-lg bg-brand-50 text-brand-600"><FileText size={18}/></span><span className="min-w-0 flex-1"><span className="mb-1 flex flex-wrap items-center gap-1.5"><span className="rounded-md bg-slate-100 px-2 py-0.5 text-[11px] font-bold text-slate-600">{notice.category || '기타'}</span><span className={`rounded-md px-2 py-0.5 text-[11px] font-bold ${status.className}`}>{status.label}</span>{notice.isSample&&<span className="rounded-md bg-amber-100 px-2 py-0.5 text-[11px] font-bold text-amber-900">샘플</span>}</span><strong className="block text-sm leading-5 text-slate-800">{notice.title}</strong><small className="mt-1 block text-xs text-slate-500">게시 {notice.publishedAt.slice(0,10)}</small></span></div>{notice.evidenceExcerpt&&<blockquote className="mt-3 border-l-2 border-brand-200 pl-3 text-xs leading-5 text-slate-600">{notice.evidenceExcerpt}</blockquote>}<a href={notice.sourceUrl} target="_blank" rel="noreferrer" className="mt-3 inline-flex min-h-11 cursor-pointer items-center gap-1.5 rounded-lg px-2 text-sm font-bold text-brand-700 transition hover:bg-brand-50 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-600">{notice.isSample?'샘플 상세 보기':'원문 보기'}<ExternalLink size={15}/></a></article>
}
